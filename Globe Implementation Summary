# 🌍 首页巨型旋转地球 (COBE) - 技术总结文档

本页面详细记录了 `Sanctuary Project` 首页引入 3D 点阵旋转地球背景元素的实现逻辑与具体技术细节。

## 1. 核心选型与架构

- **所选类库**：`cobe` (轻量级的 2D Canvas 绘制 3D 效果的地球库)
- **体积影响**：新增包大小仅约 `~5KB` (Gzipped)，按需懒加载 (Lazy Load) 以防阻塞首屏。
- **依赖声明**：直接依赖，无重度 WebGL 或 Three.js 引入，渲染性能稳定。
- **兼容性**：支持 `prefers-reduced-motion`，并在所有现代浏览器流畅稳定 (约 60FPS)。

## 2. 视觉定位与布局策略

地球未居中，而是锚定在视口右下角（Bottom-Right），以此呈现一种从边缘“探出”画面的巨大天体感：

### A. 容器布局 (`Hero.tsx`)

| CSS 属性 | 设定值 | 目的 |
|----------|--------|------|
| `position` | `absolute` | 脱离正常文档流，悬浮在 Hero 容器内 |
| `bottom` | `-15%` | 向下溢出容器，切掉底部边缘 |
| `right` | `-15%` | 向右溢出容器，切掉右侧边缘 |
| `opacity` | `0.18` | 极高透明度，作为背景融入 `mimo-bg`，不干扰标题阅读 |
| `z-index` | `0` | 在主标题 `z:10` 之下 |
| `pointer-events` | `auto` | 保留地球自身的鼠标交互能力（拖拽旋转） |

### B. 尺寸计算逻辑 (响应式)

根据屏幕尺寸动态计算边长（即 Canvas 大小），并始终保持至少覆盖 50% 以上尺寸：
长宽基准值 `base = Math.max(vw, vh)`。

- **手机端 (`vw < 640`)**：`Size = base * 0.85`
- **平板端 (`vw < 1024`)**：`Size = base * 0.75`
- **桌面端 (`vw >= 1024`)**：`Size = base * 0.8`

> 结合 `-15%` 偏移和巨大投影 `width/height`，地球占据屏幕近半面，且自然与主标题发生透传重叠。

## 3. 地球材质与表现 (`Globe.tsx`)

### 渲染参数设置

- **DevicePixelRatio**：限制至最高 `2`，防止超高分屏（如 4K+ / Retina）出现性能问题。
- **phi/theta**：`phi` 起始自 `0`，自转速度 `0.002` Radian/Frame，倾角 `theta: 0.25`。
- **暗/亮模式 (`dark`)**：设为 `0` (亮色模式)，保持与前台白色背景协调。
- **Diffuse (漫反射强度)**：`1.4` 让光感更柔和。
- **点阵质量 (`mapSamples`)**：`20,000` (点数)，高密度带来更细腻边界。
- **陆地亮度 (`mapBrightness`)**：`6`。

### 配色表 (与 MiMo 对接)

- **基底颜色 (BaseColor)**：`[0.93, 0.93, 0.93]` (极为接近底层 `mimo-bg`)
- **光晕发光 (GlowColor)**：`[0.88, 0.88, 0.92]` (偏冷的柔光)
- **网点颜色 (Marker/Dot Color)**：`[0.25, 0.25, 0.28]` (近黑但含一丝冷蓝)

### 地理坐标节点 (Markers)

地球表面散列绘制了这 7 个主要地理节点，以突显“避难所/全球链接”的 Web3 协议隐喻：

1. **上海** `[31.2304, 121.4737]` (半径 `0.06`)
2. **北京** `[39.9042, 116.4074]`
3. **香港** `[22.3193, 114.1694]`
4. **东京** `[35.6762, 139.6503]`
5. **旧金山** `[37.7749, -122.4194]`
6. **伦敦** `[51.5074, -0.1278]`
7. **新加坡** `[1.3521, 103.8198]`

## 4. 交互式状态机

除了缓慢自转 `phi += 0.002` 外，支持用户干预并接管物理转动：

- **PointerDown**：记录用户按下的初始 `clientX`，将鼠标设为 `grabbing`
- **PointerMove**：`delta = clientX - pointerInteracting.current`，将差值除以 `200` 用于改写旋转角度。
- **PointerUp/Out**：恢复状态为 `null`，复原游标为 `grab`。自然回归到定速自转。

## 5. 加载优化体验

1. **组件按需懒加载**：在 `Hero.tsx` 当中使用 `React.lazy` 与 `<Suspense>` 进行挂载加载，确保首屏 HTML/CSS TTI（Time to Interactive）达到最快。
2. **CSS Fade-in**：Globe 在首次挂载时使用内联控制方案 `canvasRef.current.style.opacity = '1'` 设置 1.5s 柔和淡入（从 0 到 1），避免加载完地球由于体积大忽然出现的突兀闪烁。
3. **无障碍设计 (a11y)**：

- `prefers-reduced-motion: reduce` 监测，如用户系统设定减速运动，强制使 `phiRef.current` 增项不再累积，实现静态悬浮。
- `aria-hidden="true"` 防止阅读器将复杂动画层解读出过多噪音内容。

## 6. 文件与组件地图

- `components/Globe.tsx` (新创建的地球核心组件)
- `components/Hero.tsx` (修改了原有的文案背景层 `<div className="text-[15vw]...">`，变更为 `<Suspense fallback={null}><Globe /></Suspense>`)

---
**完结**：项目在增加庞大沉浸感背景元素的同时，依旧保证了代码整洁性与前端极速性能。
